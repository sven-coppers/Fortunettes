/**
 * Default constructor
 * @constructor
 */
/*function SliderState() {
    this.visible = null;
    this.enabled = null;
    this.leftValue = null;
    this.rightValue = null;
}

SliderState extends WidgetState();
SliderState.prototype.extendPrototype({
    /**
     * check if two instances of widgetState are equal
     * @param otherWidgetState
     *
    equals(otherWidgetState) {
        if(this.visible != null && otherWidgetState.visible != null) {
            if(this.visible != otherWidgetState.visible) return false;
        }

        if(this.enabled != null && otherWidgetState.enabled != null) {
            if(this.enabled != otherWidgetState.enabled) return false;
        }

        if(this.leftValue != null && otherWidgetState.leftValue != null) {
            if(this.leftValue != otherWidgetState.leftValue) return false;
        }

        if(this.rightValue != null && otherWidgetState.rightValue != null) {
            if(this.rightValue != otherWidgetState.rightValue) return false;
        }

        return true;
    }

   /**
     * Create a copy of this state
     *
    getCopy() {
        var result = new SliderState();

        result.visible = this.visible;
        result.enabled = this.enabled;
        result.leftValue = this.leftValue;
        result.rightValue = this.rightValue;
        result.copyChildrenStates(this);

        return result;
    }
});
*/ 
//# sourceMappingURL=slider_state.js.map